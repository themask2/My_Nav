<Page
    x:Class="Navegador.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Navegador"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="15*"/>
        </Grid.RowDefinitions>
        <Grid Background="#bbbbaa">
            <StackPanel Orientation="Horizontal" Margin="15,0,15,0" Width="150" HorizontalAlignment="Left">
                <Button Click="Voltar_Pagina">Voltar</Button>
                <Button Click="Avancar_Pagina">Avancar</Button>
            </StackPanel>
        </Grid>
        <Grid Grid.Row="1">
            <WebView x:Name="Home" Source="http://google.com" />
        </Grid>
    </Grid>
    
</Page>
